import {sumTwoNum, printConsole} from './util.js'
import subTwoNum from './util.js'

 function main() {
    printConsole("tong 2 so 1 & 2:" +  sumTwoNum(1, 2));
    printConsole("hieu 2 so 1 & 2:" +  subTwoNum(1, 2));
 }
 main( );